We ran our unit tests, here's the output:

---
{{ sh('cat tox_output.log') }}
---

If the tests are green, do nothing. Otherwise:
1. Read the PR #{{ env('PR_NUMBER') }} to get a better understanding of the context
2. Comment on the PR and point out the changes that cause the tests to fail